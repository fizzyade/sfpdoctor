{
  "name": "sfpdoctor",
  "version": "1.0.0",
  "description": "A program for reading and writing the EEPROM on SFP/SFP+ modules.",
  "main": "app.ts",
  "author": "Adrian Carpenter",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "lint": "tslint -c tslint.json -p tsconfig.json",
    "start": "npm run build && cd build/dist && electron ./app.js"
  },
  "devDependencies": {
    "electron": "^6.0.10",
    "electron-rebuild": "^1.8.6"
  },
  "dependencies": {
    "@types/usb": "^1.5.1",
    "typescript": "^3.6.3",
    "usb": "^1.6.0"
  }
}
